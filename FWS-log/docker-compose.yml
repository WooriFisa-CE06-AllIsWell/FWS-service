services:
  postgres:
    image: postgres:16
    container_name: fws-log-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: fws_logs
      POSTGRES_USER: fws
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "5432:5432"
    volumes:
      - /nfs/shared-log/pgdata:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql

  log-server:
    build: .
    container_name: fws-log-server
    restart: unless-stopped
    ports:
      - "9000:9000"
    env_file:
      - .env
    depends_on:
      - postgres
